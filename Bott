from telegram import Update, ReplyKeyboardMarkup
from telegram.ext import Updater, CommandHandler, MessageHandler, Filters, CallbackContext

from shops import get_conversation_handler  # импортируем весь сценарий скидок
from db import init_db

TOKEN = "ВАШ_ТОКЕН_ЗДЕСЬ"  # вставьте свой токен

# Главное меню кнопки
MAIN_MENU = [["➕ Добавить скидку", "🔍 Найти скидку", "💰 KWT / Бар"]]

def start(update: Update, context: CallbackContext):
    """Старт бота и главное меню"""
    context.user_data.clear()
    update.message.reply_text(
        "Добро пожаловать 👋\nВыберите действие:",
        reply_markup=ReplyKeyboardMarkup(
            MAIN_MENU,
            resize_keyboard=True,
            one_time_keyboard=True
        )
    )

def main():
    print(">>> MAIN STARTED <<<")
    init_db()
    updater = Updater(TOKEN, use_context=True)
    dp = updater.dispatcher

    # Команда /start
    dp.add_handler(CommandHandler("start", start))

    # 🔥 Подключаем полностью conversation handler из shops.py
    dp.add_handler(get_conversation_handler())

    # Убираем любые отдельные MessageHandler на "➕ Добавить скидку", иначе зацикливание
    # dp.add_handler(MessageHandler(Filters.regex("^➕ Добавить скидку$"), start_discount))  <- убрать!

    # Старт polling
    updater.start_polling()
    print(">>> POLLING START <<<")
    updater.idle()

if __name__ == "__main__":
    main()
