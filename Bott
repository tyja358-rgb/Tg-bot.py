from telegram import ReplyKeyboardMarkup, ReplyKeyboardRemove, ConversationHandler

def finish(update, context):
    d = context.user_data

    # 1️⃣ Сообщение о добавленной скидке — БЕЗ кнопок
    update.message.reply_text(
        f"✅ Скидка добавлена\n"
        f"🏬 {d['store']}\n"
        f"💰 {d['before']} → {d['after']} {d['currency']}\n"
        f"📊 {d['discount']:.1f}%\n"
        f"📍 {d['address']}",
        reply_markup=ReplyKeyboardRemove()
    )

    # 2️⃣ Реклама — БЕЗ кнопок
    update.message.reply_text(
        "🔥 Реклама 🔥\n"
        "Лучшие скидки и купоны каждый день\n"
        "➡️ @kwt_deals"
    )

    # 3️⃣ Возврат в главное меню
    update.message.reply_text(
        "Выберите действие:",
        reply_markup=ReplyKeyboardMarkup(
            [["➕ Добавить скидку", "🔍 Найти скидку", "💰 KWT / Бар"]],
            resize_keyboard=True
        )
    )

    context.user_data.clear()
    return ConversationHandler.END
